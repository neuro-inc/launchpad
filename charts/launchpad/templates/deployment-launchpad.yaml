apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "launchpad.name" . }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "launchpad.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "launchpad.selectorLabels" . | nindent 8 }}
        {{- include "launchpad.apoloPodLabels" . | nindent 8 }}
    spec:
      imagePullSecrets:
        - name: "ghcr"
      containers:
        - name: launchpad
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          command:
            - "python"
            - "-m"
            - "launchpad"
          livenessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /ping
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          ports:
            - containerPort: {{ .Values.port }}
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: {{ .Values.port | quote }}
            - name: DB_HOST
              value: {{ .Values.postgresql.fullnameOverride }}
            - name: DB_USER
              value: "postgres"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.dbSecretName }}
                  key: DB_PASSWORD
            - name: DB_NAME
              value: "launchpad"
            - name: KEYCLOAK_URL
              value: {{ include "keycloak.domain" . }}
            - name: KEYCLOAK_REALM
              value: {{ .Values.keycloak.defaultRealm }}
            - name: APOLO_PASSED_CONFIG
              value: {{ .Values.APOLO_PASSED_CONFIG | quote }}
            - name: LAUNCHPAD_INITIAL_CONFIG
              value: {{ .Values.LAUNCHPAD_INITIAL_CONFIG | quote }}
            - name: SELF_DOMAIN
              value: {{ include "launchpad.apiDomain" . }}
            - name: BASE_DOMAIN
              value: {{ .Values.domain }}
            - name: AUTH_MIDDLEWARE_NAME
              value: platform-{{ include "launchpad.name" . }}-auth-middleware
