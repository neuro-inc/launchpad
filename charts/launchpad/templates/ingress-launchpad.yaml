apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "launchpad.name" . }}
spec:
  ingressClassName: traefik
  rules:
    - host: {{ include "launchpad.apiDomain" . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "launchpad.name" $ }}
                port:
                  name: http
